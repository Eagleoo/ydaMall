<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:background="#ffffffff"
    android:gravity="fill"
    android:isScrollContainer="true"
    android:orientation="vertical">

    <include
        android:id="@id/classListTop"
        layout="@layout/title_blue" />

    <LinearLayout
        android:id="@+id/huodong_banner"
        android:layout_width="fill_parent"
        android:layout_height="120dp"
        android:layout_below="@id/classListTop"
        android:orientation="horizontal">

        <ImageView
            android:layout_width="fill_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="top"
            android:scaleType="centerCrop"
            android:src="@drawable/huodong_banner" />

    </LinearLayout>

    <ListView
        android:id="@id/prodShow"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/huodong_banner"
        android:cacheColorHint="#ffffffff"
        android:divider="#ffefefef"
        android:dividerHeight="1.0dip"
        android:visibility="visible" />


</RelativeLayout>